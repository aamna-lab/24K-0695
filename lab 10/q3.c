#include<stdio.h>
int compare(float t1){}
int main(){
    int ans,choice;
    float temp;
    const int maxT= 1000;
    int compare(float t1){
    int count;
    if(t1>maxT){
        count++;
    }
   
}
do{
printf("Enter '1' to enter temp or '0' to exit\n");
scanf("%d", &choice);
printf("Enter temperature\n");
        scanf("%f", &temp);
}while(choice==1);
        ans= compare(temp);
}
